ul.pagination-links
    while pageLinks <= pages
        li
            if term
                a(href=`/books/search?term=${term}&page=${pageLinks}`)= pageLinks++
            else
                a(href=`/books/?page=${pageLinks}`)= pageLinks++